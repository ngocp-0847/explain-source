# QA Chatbot Backend Configuration
# Copy this file to .env and adjust values as needed

# =============================================================================
# Agent Selection
# =============================================================================
# Choose which code analysis agent to use (gemini or cursor)
# Default: gemini
AGENT_TYPE=gemini

# =============================================================================
# Gemini CLI Configuration
# =============================================================================
# Path to Gemini CLI executable
# Default: "gemini" (assumes it's in PATH)
GEMINI_AGENT_PATH=gemini

# Analysis timeout in seconds
# Default: 300 (5 minutes)
GEMINI_AGENT_TIMEOUT=300

# Maximum number of retry attempts on failure
# Default: 2
GEMINI_AGENT_MAX_RETRIES=2

# Working directory for code analysis (optional)
# If not set, will use project directory from database
# GEMINI_AGENT_WORKING_DIR=/path/to/your/project

# Output format: text, json, stream-json, stream-partial
# Default: stream-json (recommended for real-time updates)
# GEMINI_AGENT_OUTPUT_FORMAT=stream-json

# Gemini API key (optional, for future use)
# GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# Cursor Agent Configuration
# =============================================================================
# Path to Cursor Agent executable
# Default: "cursor-agent" (assumes it's in PATH)
CURSOR_AGENT_PATH=cursor-agent

# Analysis timeout in seconds
# Default: 300 (5 minutes)
CURSOR_AGENT_TIMEOUT=300

# Maximum number of retry attempts on failure
# Default: 2
CURSOR_AGENT_MAX_RETRIES=2

# Working directory for code analysis (optional)
# If not set, will use project directory from database
# CURSOR_AGENT_WORKING_DIR=/path/to/your/project

# Output format: text, json, stream-json, stream-partial
# Default: stream-json (recommended for real-time updates)
# CURSOR_AGENT_OUTPUT_FORMAT=stream-json

# Cursor API key (optional)
# CURSOR_API_KEY=your_cursor_api_key_here

# =============================================================================
# Database Configuration
# =============================================================================
# SQLite database file path
# Default: sqlite:qa_chatbot.db
DATABASE_URL=sqlite:qa_chatbot.db

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. For Gemini CLI (default):
#    - Install: npm install -g @google/generative-ai-cli
#    - Login: Run 'gemini' interactively and complete OAuth
#    - Set AGENT_TYPE=gemini (or leave default)
#
# 3. For Cursor Agent:
#    - Install Cursor CLI: curl https://cursor.com/install -fsS | bash
#    - Set AGENT_TYPE=cursor
#    - Set CURSOR_AGENT_PATH if not in PATH
#
# 4. Run the backend:
#    cargo run
